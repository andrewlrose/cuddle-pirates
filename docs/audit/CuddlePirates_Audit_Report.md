# CuddlePirates — Audit Report

*Fable creative/business portfolio audit — deep pass, 2026-07-07. Supersedes the 2026-07-07 19:36 shallow draft.*

## Executive Summary

CuddlePirates is a CREATIVE/SOFTWARE hybrid, but heavily weighted creative: the "software" layer is a stub `package.json` (v0.0.1, no scripts, no dependencies) and an `index.html`, while the substance is a 1,266-line story bible, a curated 97-image Midjourney concept inventory, storyboards, a spec script, and pitch analysis — all serving its role as the first demo IP proving out Volta's pipeline for a Q3 2026 pitch. The project's documentation discipline is unusually good (locked decisions recorded, open blockers explicitly listed, "do not invent answers" rules). The deep findings are about drift and weight: the bridge manifest declares only Volta, with no Writers_Room wiring despite ADR-0009 expectations; the repo carries ~805MB with 258 images/zips tracked in git — including an apparently duplicated `docs/CuddlePirates_updates/` snapshot — against the project's own "reference, don't duplicate" rule; family email files (.eml) are only half-quarantined, with the deletions still uncommitted; and the story bible's own status contradicts CLAUDE.md's "placeholder until Section G" rule. Per audit guardrails, **no narrative content was touched** — everything below is flag-only.

## Findings

| # | Issue | Location | Category | Severity | Recommended action | Status |
|---|-------|----------|----------|----------|--------------------|--------|
| CP-1 | **No Writers_Room wiring despite being a declared consumer.** `.atlas-bridge/manifest.json` lists `consumes/depends_on: ["Volta"]` only; no Writers_Room entry, no adapter, no `writers-room.config.json`. Story-bible canon (1,266 lines, some marked "locked") is managed as loose Markdown with none of the engine's proposal/approval/provenance machinery. Mirrors Writers_Room finding WR-9. | `.atlas-bridge/manifest.json`, `story-bible/` | writers-room-integration | **Medium** | Decide: either wire CuddlePirates into Writers_Room (adopt + adapter, engine work per ADR-0009) before more canon accumulates, or update the portfolio contract so Volta is its only declared engine. Don't leave it ambiguous. | Flagged — cross-project decision |
| CP-2 | **Repo weight contradicts the project's own reference-only rule.** Working tree ~805MB; `concept/intake/` alone is 416MB inside the repo (partially gitignored, but 258 png/jpg/zip files ARE git-tracked, including `docs/CuddlePirates.zip` and an unpacked duplicate of it at `docs/CuddlePirates_updates/`). CLAUDE.md: "Don't duplicate concept PNGs into the project tree." | `concept/intake/`, `docs/CuddlePirates_updates/`, `docs/CuddlePirates.zip` | asset-organization | **Medium** | Human decision: keep the zip OR the unpacked copy (not both); move intake drop-zone bulk to the OneDrive reference roots + inventory entries. No files were moved or deleted by this audit (creative-content guardrail). | Flagged — creative content, never auto-deleted |
| CP-3 | **Family emails half-quarantined; deletions uncommitted.** 6 `.eml` files (real family story-notes correspondence) are git-tracked; S199-U21 moved 4 into `.eml_quarantine/` but the removals of the old `concept/intake/Story/` copies show as uncommitted `D` in the working tree, and all copies remain in git history. The `.gitignore` header says `.atlas-bridge/` is excluded because "not for public repos" — implying possible future publication of this repo. | `.eml_quarantine/`, `concept/intake/Story/` (deleted, uncommitted) | ip-rights / personal data | **High** | Commit the quarantine deletions natively; keep this repo private permanently OR history-scrub the emails before any publication. Contents intentionally not quoted in this report. | Flagged — personal data + git ops are Andy-native |
| CP-4 | **Canon status contradiction: "placeholder" vs "locked."** CLAUDE.md says story-bible files "stay placeholder until Section G fills" and "Don't write story-bible canon yet" — but `story-bible/characters.md` declares "Core-5 character profiles **locked** … 2026-04-25 (S39)" with 1,266 lines of developed material (Ann Bunny deep-dev, sock mechanic, world, tone). Also the project's own flagged-and-unresolved K3 conflict: "MetaHuman for hero characters" vs the locked stylized-painterly style. | `CLAUDE.md` vs `story-bible/*.md`, `docs/round2-answers.md` (K3) | canon-continuity | **Medium** | Human reconciliation: update CLAUDE.md to reflect what's actually locked, and resolve K3 when Section G is answered. Read-and-report only — no narrative files edited. | Flagged — canon is human-only |
| CP-5 | **Portability rule half-implemented: hashes never computed.** `concept/inventory.json` v1.1 has the right `file_refs` shape (`{sha256, relative_path}` + named `source_roots`) — but every `sha256` is `null`, and the legacy absolute `E:/OneDrive/...` paths remain the primary reference. If OneDrive reorganizes, nothing can verify which file was which. | `concept/inventory.json` | data-ownership | **Medium** | Run a one-off hashing pass to fill the `sha256` fields (Volta's REFERENCE-tier import can do this when the `.volta/` catalog migration ships — see Volta V-1/V-6). | Flagged — file I/O |
| CP-6 | **Concept source-of-truth lives in a 2023 OneDrive backup folder.** `E:/OneDrive/Backup_2023oct22/AI` + `/Desktop` are the declared source roots for the pitch's visual identity — a cloud-synced folder named like a one-time backup, with no second copy inventoried. | `concept/inventory.json` `source_roots` | asset-organization / data-ownership | **Medium** | Add these two folders to the household backup target (easystore/M6) and note the copy in the inventory. What breaks if OneDrive account access is lost: the original 2023 concept-art vintage, unrecoverable. | Flagged |
| CP-7 | **IP provenance of AI-generated pitch art needs a documented human decision.** All concept art is Midjourney 2023 (documented — good); pitch usage of AI-generated imagery, MJ's commercial terms for the account tier used, and character designs "based on caricatures of Raylan's uncles and grandpas" (real-person likenesses) are all fine family-internally but need a one-paragraph rights note before external pitching. | `concept/`, story-bible character basis notes | ip-rights | Low | Write a short provenance/rights note into the pitch appendix (tool, dates, account tier, likeness consent). No legal judgment made here — just surfacing the decision. | Flagged — human decision |
| CP-8 | **Uncommitted working-tree changes.** Beyond CP-3's deletions, several `M` entries (CLAUDE.md, READMEs, `.volta/README.md`, `.gitignore`) — some may be the documented CRLF/mount artifact, some may be real. | repo root | deps-tests | Info | Verify with native `git status`/`git diff`; commit the real ones with the audit deliverables. | Flagged for native verification |
| CP-9 | **Positive: decision hygiene is exemplary.** Locked decisions cite their source and session (e.g. style lock, Round 2 M2/M3), open blockers are explicit, and "what NOT to do" rules exist. The `.volta/` per-project catalog placeholder is already in place, correctly anticipating Volta's V-1 fix. | `CLAUDE.md`, `docs/round2-answers.md`, `.volta/README.md` | architecture | Info | Keep the pattern; it's the reason this audit could be precise. | No action needed |

### Plain-language layer, finding by finding

- **CP-1:** This project was supposed to plug its story decisions into the family's shared story engine, which enforces "a human must approve everything." Instead the story lives in plain files with no approval trail. Fine for now — but every month of drift makes plugging in later more painful. Pick a lane deliberately.
- **CP-2:** The project folder weighs nearly a gigabyte because concept images live inside it — including the same batch of files stored twice (zipped *and* unzipped). The project's own rulebook says images should be referenced from OneDrive, not copied in. Nothing was deleted; that's a human call.
- **CP-3:** Real family emails are in this repo. Someone already started moving them into a quarantine folder — but the move was never finished (the deletions aren't committed), and git remembers everything ever committed. If this repo is ever made public, the emails come with it. Finish the move; keep the repo private.
- **CP-4:** The instruction file says "the story bible is empty placeholder — don't write canon yet," but the story bible actually contains a locked five-character cast and hundreds of lines of world-building. Both can't be true. Also, one locked answer says "use MetaHuman" (photoreal humans tech) while the locked art style is soft storybook — the project flagged this itself and it's still unresolved.
- **CP-5:** The image catalog was built to identify pictures by fingerprint so they survive being moved — but the fingerprints were never filled in. It's a padlock with no key cut yet.
- **CP-6:** Every piece of the show's visual identity traces back to one cloud folder named "Backup_2023oct22." One folder, one account, no second copy. Cheap fix: copy it to the family backup drive.
- **CP-7:** The pitch art is AI-generated and the characters are affectionately based on real relatives. Before showing outsiders, write one paragraph saying who owns what and that the family's fine with the likenesses. Boring paperwork now beats awkward questions in a pitch meeting.
- **CP-8:** Some files show as changed in ways that may be the known Windows/Linux illusion. One native `git status` sorts it out.
- **CP-9:** Genuine compliment: this project writes down its decisions, its open questions, and its own rules better than most professional productions.

## Evolution Roadmap

| Priority | Recommendation | Tags | Why this matters (plain language) |
|----------|----------------|------|-----------------------------------|
| 1 | Finish the .eml quarantine: commit deletions, confirm repo stays private (CP-3) | [Data-Ownership] | Family correspondence shouldn't be one `git push --public` away from the internet. |
| 2 | Resolve the Writers_Room question — wire in or formally opt out (CP-1) | — | The story bible is growing; every week decides this by default instead of on purpose. |
| 3 | De-duplicate `docs/` snapshots + relocate intake bulk per the reference rule (CP-2) | [Data-Ownership] | Shrinks an 805MB repo dramatically and makes backups/clones practical. |
| 4 | Hash the concept inventory + back up the OneDrive source roots (CP-5, CP-6) | [Data-Ownership] | The show's entire visual DNA becomes verifiable and survivable. |
| 5 | Reconcile CLAUDE.md vs story-bible status; resolve K3 with Section G (CP-4) | — | An instruction file that contradicts reality misleads every future AI session that reads it. |
| 6 | One-paragraph AI-art/likeness rights note in pitch materials (CP-7) | — | Cheap insurance before external eyes see the deck. |
| 7 | When Volta's per-project catalog ships, import concept refs as REFERENCE-tier (`sensitivity: yellow` on any vault export) | [Local-First] | Keeps pre-pitch IP off external AI services, per the household embedding split. |

## Change List

- **No changes to any narrative, concept, or pitch content** (guardrail: canon approval is human-only; creative directories are flag-only even for formatting).
- No code changes — the only code is a stub `package.json`/`index.html` with nothing to fix.
- Rewrote `docs/audit/` deliverables (this report, Overview, Operating Guide + HTML twins), replacing the shallow drafts.

## Verification Notes

- Type confirmed CREATIVE-dominant hybrid: `package.json` v0.0.1 stub (no scripts/deps); story-bible 7 files / 1,266 lines; concept inventory 97 images across documented archetypes.
- Git: 319 tracked files; 258 tracked images/zips; 6 tracked `.eml`; quarantine deletions present but uncommitted (sandbox `git status`, needs native confirmation).
- `concept/inventory.json` read directly: `source_roots` OneDrive paths, all `sha256: null`.
- Email contents were **not** read into this audit beyond filenames, and are not quoted anywhere, per the personal-data guardrail.
